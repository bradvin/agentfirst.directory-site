{
  "name": "agentfirst-directory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler types && astro dev",
    "start": "wrangler types && astro dev",
    "build": "wrangler types && astro build",
    "preview": "wrangler types && astro preview",
    "test": "node --test",
    "verify:rendered": "node verification/rendered-ui.mjs",
    "ci": "npm test && npm run build"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.4",
    "astro": "6.0.8",
    "markdown-it": "^14.1.1"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "typescript": "6.0.2",
    "wrangler": "4.77.0"
  }
}
